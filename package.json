{
  "name": "@vanillaes/jqkill",
  "version": "1.1.1",
  "description": "Locate all references to jQuery for easy removal",
  "keywords": [
    "jquery",
    "nodejs",
    "cli",
    "developer-tools"
  ],
  "repository": "https://github.com/vanillaes/jqkill",
  "author": "Evan Plaice <evanplaice@gmail.com> (https://evanplaice.com/)",
  "license": "MIT",
  "type": "module",
  "bin": {
    "jqkill": "bin/jqkill.js"
  },
  "exports": {
    ".": {
      "import": "./src/index.js",
      "types": "./src/index.d.ts"
    }
  },
  "scripts": {
    "test": "es test",
    "lint": "es lint",
    "type": "es type  --types node && es type --types node bin/jqkill.js",
    "build": "es typings --types node",
    "clean": "es clean --typings",
    "preview": "es preview",
    "preversion": "npm test && npm run lint && npm run type",
    "version": "npm run clean && npm run build && git add --ignore-errors **/*.d.ts",
    "postversion": "git push --follow-tags"
  },
  "dependencies": {
    "@vanillaes/esmtk": "^2.7.2",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  },
  "lint": {
    "ignore": [
      "**/__test__/*"
    ]
  }
}
